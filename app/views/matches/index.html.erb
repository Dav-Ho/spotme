
<div class="page-wrapper">


    <div class="row">
        <div class="col-lg-12">
            <center><h1 class="page-header">Your Matches
            </h1></center>
        </div>
    </div>

    <section class="stats">
      <%= render 'shared/stats' %>
    </section>

    <!-- <% @users.each do |user| %>
    <div class="col-md-4">
    <div class= "index_format">
      <h3>User_name: <%= link_to user.user_name, "/matches/#{user.id}" %></h3>
      <h4>Age: <%= user.age %></h4>
    </div>
    </div>
    <% end %>
    </div> -->

    <!-- <div class="clearfix"> -->
    <% @users.find_each do |user| %>
        <div class="col-lg-4 col-xs-6 text-center">
            <img class="img-circle img-responsive img-center" src="http://placehold.it/200x200" alt="">
            <h3>User_name: <%= link_to user.user_name, "/users/#{user.id}" %></h3>
            <h4>Age: <%= user.age %></h4>
        </div>
    <% end %>
    <!-- </div> -->
</div>
